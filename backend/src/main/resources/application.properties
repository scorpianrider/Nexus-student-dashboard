spring.application.name=student-dashboard

spring.datasource.url=jdbc:mysql://localhost:3306/student
spring.datasource.username=root
spring.datasource.password=#ARsqlracer2445
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080

# CORS for the Vite dev server (http://localhost:5173) is configured in
# com.nexus.studentdashboard.config.WebConfig — these properties alone
# would NOT enable CORS, so they've been removed from here.
